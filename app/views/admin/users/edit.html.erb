<br>

<%= render partial: '/layouts/form_errors', locals: {model: @target_user} %>

<br>

<%= form_for @target_user, :url => admin_user_path(@target_user) do |f| %>

  <%= render partial: '/users/users', locals: {f: f} %>

<br>
<hr>
<br>
  <%= f.label :admin, "Give #{user_name(@target_user)} admin privileges?" %>
  <%= f.check_box :admin %><br><br>

 <%= f.submit %><br>
<% end %>

<%= link_to "Delete #{@user.name} Forever?", admin_user_destroy_path(@user) %>
